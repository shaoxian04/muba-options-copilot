node_modules/
.env
.env.local
.env*.local
dist/
build/
.next/
out/
*.log
.DS_Store
.vercel
# never commit keys or wallet config
*.key
wallet.json
config.json

# Python (apps/agents)
.venv/
__pycache__/
*.pyc
# saved strategies are local state, not source (apps/agents/strategy/store.py)
apps/agents/data/
.claude
.worktrees/
.mcp.json
# playwright-mcp writes screenshots and snapshots here
.playwright-mcp/
# Playwright's own run output, and the scan directories the security tooling leaves behind.
# Neither was ever tracked; both sat untracked in the root where a broad `git add` would
# have swept them in.
test-results/
playwright-report/
CLAUDE-SECURITY-*/
