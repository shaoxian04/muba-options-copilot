interface:
  display_name: "Muba Options Copilot"
  short_description: "Repo-specific patterns and workflows for muba-options-copilot"
  default_prompt: "Use the muba-options-copilot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true