[pytest]
# tests import `strategy.*` as an absolute package, same as server.py does
# when run from this directory -- put apps/agents on sys.path the standard
# pytest way instead of every test file hand-rolling it.
pythonpath = .
testpaths = tests
